import { StringToCardsPipe } from './string-to-cards.pipe';

describe('StringToCardsPipe', () => {
  it('create an instance', () => {
    const pipe = new StringToCardsPipe();
    expect(pipe).toBeTruthy();
  });
});
